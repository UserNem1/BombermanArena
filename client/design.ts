/**
 * Espace de conception de l'interface.
 *
 * Toute l'interface est dessinée dans ce repère logique fixe, puis mise
 * à l'échelle et centrée par le renderer pour s'adapter à la fenêtre.
 */

/** Largeur logique de référence de l'interface (px). */
export const DESIGN_WIDTH = 1280;

/** Hauteur logique de référence de l'interface (px). */
export const DESIGN_HEIGHT = 720;
